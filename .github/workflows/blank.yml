name: CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 SFTP Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.KEY_PASS }}
          script: |
            # SFTP commands to upload files
            cd /var/www/node25.webte.fei.stuba.sk/zaverecne-zadanie/
            put -r local_directory
